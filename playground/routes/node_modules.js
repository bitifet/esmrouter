var express = require('express');
var esmrouter = require('esmrouter');

var modules_options = {
    //path: "foo/bar",
    //target: "all",
    warn: false,
}

var router = esmrouter(express, modules_options);

module.exports = router;
